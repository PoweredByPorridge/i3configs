# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod mod4


# Some borrowed colours
# set $mod Mod4
set $base00 #101218
set $base01 #1f222d
set $base02 #252936
set $base03 #7780a1
set $base04 #C0C5CE
set $base05 #d1d4e0
set $base06 #C9CCDB
set $base07 #ffffff
set $base08 #ee829f
set $base09 #f99170
set $base0A #ffefcc
set $base0B #a5ffe1
set $base0C #97e0ff
set $base0D #97bbf7
set $base0E #c0b7f9
set $base0F #fcc09e

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# font pango:monospace 8
font pango:DejaVu Sans Mono 8
#font pango:Consolas 9

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec exec st
# bindsym $mod+Shift+Return i3-sensible-terminal

# start midnight commander in xfce4-terminal
# bindsym $mod+Shift+m exec st -e 'mc'


# kill focused window
bindsym $mod+Control+k kill
# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run

# start rofi binding
# bindsym $mod+d exec rofi -show run -lines 5 -eh 2 -width 100 -padding 500 -opacity "80" -bw 0 -bc "$base01" -bg "$base02" -fg "$base07" -hlbg "$base05" -font "DejaVu Sans Mono 18" -hlfg "#9575cd"
# bindsym $mod+Shift+d exec rofi
# bindsym $mod+d exec rofi -show run -lines 8 -separator-style none -eh 1 -width 40 -padding 10 -bw 0 -font "DejaVu Sans Mono 10" 
bindsym $mod+m exec rofi -show run
bindsym $mod+Shift+m  exec rofi -show window
bindsym $mod+n  exec rofi -show window

# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that.
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# mouse and focus
# Don't need to click in a window for focus, just nmouseover. Yuk.
focus_follows_mouse no

# Don't take mouse to other monitor when switching
mouse_warping none

## change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# # move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

## change container layout (stacked, tabbed, toggle split)
## Disabled because I never used them, and I wanted the
## shortcut for workspaces. Now I want to use them and have to
## find other shortcuts

# bindsym $mod+s layout stacking
bindsym $mod+Tab layout toggle tabbed split
# bindsym $mod+w layout tabbed
# bindsym $mod+Shift+s layout toggle split


# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+c focus child

## Modified workspace stuff

set $workspace1 "1: Stuff "
set $workspace2 "2: Emacs "
set $workspace3 "3: Firefox "
set $workspace4 "4: Subl "
set $workspace5 "5: PDF "
set $workspace6 "6: Music "
set $workspace7 "7: OpenOffice "
set $workspace8 "8: Zath "
set $workspace9 "9: Files "
set $workspace10 "0: Ternimals >_"

############


# This option determines which border style new windows will have. The default is normal. Note that new_float applies only to windows which are starting out as floating windows, e.g., dialog windows, but not windows that are floated later on.
# Syntax:

# new_window normal|none|pixel
# new_window normal|pixel <px>
# new_float normal|none|pixel
# new_float normal|pixel <px>

# Example:
new_window pixel

# The "normal" and "pixel" border styles support an optional border width in pixels:
# Example:
# # The same as new_window none
# new_window pixel 0
# # A 3 px border
# new_window pixel 3
# 4.11. Hiding borders adjacent to the screen edges
# You can hide container borders adjacent to the screen edges using hide_edge_borders. This is useful if you are using scrollbars, or do not want to waste even two pixels in displayspace. Default is none.
# Syntax:
# hide_edge_borders none|vertical|horizontal|both
# also smart - hides borders when there is only one window
# Example:
# hide_edge_borders vertical	
############
hide_edge_borders smart


# class                 border  backgr. text    indicator child_border
client.focused          #ff8000 #285577 #ffffff #2e9ef4   #ff8000
client.focused_inactive #ff8000 #5f676a #ffffff #484e50   #5f676a
client.unfocused        #333333 #222222 #888888 #292d2e   #222222
client.urgent           #2f343a #900000 #ffffff #900000   #900000
client.placeholder      #000000 #0c0c0c #ffffff #000000   #0c0c0c

client.background       #ffff00

# switch to workspace
bindsym $mod+q workspace $workspace1
bindsym $mod+w workspace $workspace2
bindsym $mod+e workspace $workspace3
bindsym $mod+r workspace $workspace4
bindsym $mod+t workspace $workspace5
bindsym $mod+y workspace $workspace6
bindsym $mod+u workspace $workspace7
bindsym $mod+i workspace $workspace8
bindsym $mod+o workspace $workspace9
bindsym $mod+p workspace $workspace10

# In workspace x, do mod+2 to get to emacs, and then do mod+2 again
# to get back to workspace x again. That is, back to where you came from.
workspace_auto_back_and_forth yes

# move focused container to workspace
bindsym $mod+Shift+q move container to workspace $workspace1
bindsym $mod+Shift+w move container to workspace $workspace2
bindsym $mod+Shift+e move container to workspace $workspace3
bindsym $mod+Shift+r move container to workspace $workspace4
bindsym $mod+Shift+t move container to workspace $workspace5
bindsym $mod+Shift+y move container to workspace $workspace6
bindsym $mod+Shift+u move container to workspace $workspace7
bindsym $mod+Shift+i move container to workspace $workspace8
bindsym $mod+Shift+o move container to workspace $workspace9
bindsym $mod+Shift+p move container to workspace $workspace10

## Auto move stuff to the right place

assign [class="Firefox"] $workspace1
assign [class="Emacs"] $workspace2
assign [class="Sublime_text"] $workspace7
assign [class="calibre"] $workspace7
assign [class="electron-mail"] $workspace4
assign [class="Zathura"] $workspace8
assign [class="Master PDF Editor"] $workspace5
assign [class="Evince"] $workspace5
for_window [class="Spotify"] move to workspace $workspace6
assign [class="X-Plane"] $workspace8

## Move workspaces to monitors

bindsym $mod+Shift+greater move workspace to output right
bindsym $mod+Shift+less move workspace to output left

# bindsym $mod+x move workspace to output right
# bindsym $mod+Shift+greater move container to output right
# bindsym $mod+Shift+less move container to output left


### My stuff
## Set up screens
# exec --no-startup-id /home/dtweed/.screenlayout/default-two-LP.sh
# exec --no-startup-id /home/dtweed/.screenlayout/default-three-middle-portrait.sh
exec --no-startup-id /home/dtweed/.screenlayout/default-three-middle-landscape.sh
# exec --no-startup-id /home/dtweed/.screenlayout/default-three-right-portrait.sh
# exec --no-startup-id /home/dtweed/.screenlayout/default-three-landscape.sh

## Set wallpaper
exec_always feh --bg-scale /home/dtweed/Pictures/trees.png

## Start dropbox
exec --no-startup-id ~/.dropbox-dist/dropboxd

## Start pulseaudio
exec /usr/bin/pulseaudio --start --daemonize

## Start nm-applet
exec --no-startup-id nm-applet --tray

## Start Python (3) webserver in Pages/public
exec_always --no-startup-id python -m http.server --directory /home/dtweed/Pages/public 2> http.server.log &

# i3 info message
# 202 is F24, which is super A. Super is on the left for keyboards without a super.
bindcode 202 exec --no-startup-id /home/dtweed/Dropbox/config/i3/i3-commands-message.sh
# 201 is F23
bindcode 201 exec --no-startup-id /home/dtweed/Dropbox/config/i3/blank-screen.sh
# i3 help message,now that I've changed all the keys.
# Left control
# bindcode 37 exec --no-startup-id /home/dtweed/Dropbox/config/i3/control-message.sh
# Left shift
# bindcode 50 exec --no-startup-id /home/dtweed/Dropbox/config/i3/control-message.sh

# exec --no-startup-id xset dpms 0 0 0	
# exec --no-startup-id xset -dpms
# exec --no-startup-id xset s off
# exec --no-startup-id xscreensaver -nosplash

# Toggle screensaver
# bindsym $mod+comma exec --no-startup-id "/home/dtweed/Dropbox/config/i3/toggle_screensaver.sh"

# Lock the screen 
# bindsym $mod+period exec xscreensaver-command -lock

## Multimedia (Spotify/CMUS) stuff
# Pulse Audio controls
#increase sound volume
bindsym XF86AudioRaiseVolume	exec --no-startup-id amixer set Master 10%+
#decrease sound volume
bindsym XF86AudioLowerVolume	exec --no-startup-id amixer set Master 10%-
# mute/unmute sound
bindsym XF86AudioMute		exec --no-startup-id amixer set Master toggle

# bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume 0 +5% #increase sound volume
# bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume 0 -5% #decrease sound volume
# bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle # mute sound


## Screen brightness controls - not on teapot!
# bindsym XF86MonBrightnessUp exec xbacklight -inc 20 # increase screen brightness
# bindsym XF86MonBrightnessDown exec xbacklight -dec 20 # decrease screen brightness

# Touchpad controls - not on teapot!
# bindsym XF86TouchpadToggle exec /some/path/toggletouchpad.sh # toggle touchpad

# Media player controls
bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioPause exec playerctl pause-pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

# reload the configuration file
bindsym $mod+Control+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Control+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Control+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+Shift+b mode "resize"
# Biggify my ternimals

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
	workspace_buttons yes
	font pango:monospace 8
#	tray_output none
#	tray_output DVI-D-1  
#       tray_output DVI-D-2
	tray_output primary
#       tray_output HDMI-1
	status_command i3blocks -c ~/Dropbox/config/i3/teapot.i3blocks.conf
	position top
	colors {
		background #000000
	}
}
